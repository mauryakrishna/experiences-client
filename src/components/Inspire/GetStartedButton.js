import React from 'react';

import { Button } from '../UIElements';

const GetStarted = () => {
  return (
    <Button position="relative" variant="solid">
      Get Started
    </Button>
  );
};

export default GetStarted;
